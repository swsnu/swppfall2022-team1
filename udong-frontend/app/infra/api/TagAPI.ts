export const TagAPI = (() => {
    function getTag() { return }
    function editTag() { return }
    function deleteTag() { return }

    return Object.freeze({
        getTag,
        editTag,
        deleteTag,
    })
})()
