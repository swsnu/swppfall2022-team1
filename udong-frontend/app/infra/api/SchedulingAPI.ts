export const SchedulingAPI = (() => {
    function getSchedulingStatus() { return }
    function closeScheduling() { return }

    return Object.freeze({
        getSchedulingStatus,
        closeScheduling,
    })
})()
